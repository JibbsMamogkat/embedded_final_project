#include "roi_cropper.h"

void extract_roi_patch(const uint8_t* full_frame, 
                       int frame_width, 
                       int x, int y, int width, int height, 
                       uint8_t* output_patch_64x64) {
    // Left empty intentionally for initial baseline compilation check
}